<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.itheima.ncp.mapper.review.ProductReviewMapper">

    <insert id="insert" useGeneratedKeys="true" keyProperty="id" keyColumn="id">
        INSERT INTO product_review (product_id, user_id, username, content, rating)
        VALUES (#{productId}, #{userId}, #{username}, #{content}, #{rating})
    </insert>

    <select id="findByProductIdOrderByCreatedAtDesc" resultType="com.itheima.ncp.entity.shop.ProductReview">
        SELECT id, product_id, user_id, username, content, rating, created_at
        FROM product_review
        WHERE product_id = #{productId}
        ORDER BY created_at DESC, id DESC
    </select>
</mapper>
